# Base URL of the backend API. For local dev against the deployed backend, set this
# to the App server's public URL (e.g. http://<app-ec2-ip>). Copy to .env (gitignored).
VITE_API_BASE=http://localhost:8000
